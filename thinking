
users
- auth
- bio

search of users

flagging users
- where does it go? admins?

blocking users
- stop nuesance users

conversations
- between two users only
- messages
  - is a resource?
- start, stop







users sign up
logs in
searches for users
starts conversation
replies to conversation
ends conbversation

çan see conversation requests
accepts conversation
cannot converse with someone who has not accepted conversations

can block users from conversation invites


pagination
  conversations
    by updated at
    limit 100
    offset by integer
  messages
    by created_at DESC
    offset by 'from date'
    limit 100
    if first message younger than conversation updated_at then there is more
